# 自动交易系统全流程执行清单（可勾选）

> 更新时间：2026-03-22
> 说明：每完成一步，把 `- [ ]` 改成 `- [x]`。
> 当前状态依据仓库现状自动预标记（文档和前端原型已完成较多，后端与交易链路尚未落地）。

## 0. 项目范围与基线（已完成）

- [x] 明确项目目标与 V1 范围（`docs/project-spec.md`）
- [x] 固化业务边界、验收口径与实施阶段（`docs/requirements-v1.md`）
- [x] 输出技术设计总览与专题设计文档（`docs/technical-design/`）
- [x] 明确前端优先实施策略（`docs/technical-design/07-web-console.md`）

## 1. Phase 1：Web 前端优先（已完成核心原型）

### 1.1 工程与骨架
- [x] 初始化前端工程（React + Vite + TypeScript）
- [x] 建立应用主框架、路由与受保护页面机制
- [x] 建立全局样式与基础组件体系

### 1.2 关键页面原型
- [x] 登录页（`/login`）
- [x] 总览页（`/overview`）
- [x] 频道管理页（`/channels`）
- [x] 日志页（`/logs`）
- [x] 人工确认页（`/manual-confirmations`）
- [x] 真实订单页（`/orders`）
- [x] 虚拟仓位页（`/virtual-positions`）
- [x] 设置页（`/settings`）

### 1.3 前端数据层（已进入可切换联调态）
- [x] 建立 mock 数据与页面状态联动
- [x] 建立高风险操作二次确认 UI（环境切换/交易开关等）
- [x] 接入真实 API 请求层（已支持 `VITE_DATA_SOURCE=api` 切换，默认仍为 mock）
- [x] 接入真实 SSE 实时流（已接入 SSE 客户端 + 断线降级轮询，依赖后端通道）

## 2. Phase 2：后端基础骨架与前后端接线

### 2.1 后端基础设施
- [x] 初始化后端工程（FastAPI）
  - 完成说明：已创建 `apps/api` 工程骨架（分层目录、依赖、启动入口、基础测试）。
- [x] 接入 PostgreSQL、Redis 基础连接
  - 完成说明：已接入 SQLAlchemy AsyncEngine 与 Redis AsyncClient，`/health/ready` 与 `/health/deps` 可返回依赖状态。
- [x] 建立配置分层（机密配置 / 业务配置 / 系统状态）
  - 完成说明：已落地 `core/config.py`，机密配置走环境变量，运行时业务配置通过 `settings/runtime` 管理。

### 2.2 认证与安全基础
- [x] 单用户登录认证（6 位密码 + 强哈希）
  - 完成说明：已实现 6 位密码校验 + Argon2 校验链路（`/api/v1/auth/login`）。
- [x] 会话管理、登录限流、失败锁定
  - 完成说明：已实现会话 cookie、服务端会话表、Redis 限流与失败锁定策略。
- [x] 基础审计记录（人工操作 vs 系统自动操作）
  - 完成说明：已实现 `operator_actions` 与 `system_logs` 写入，登录/登出/设置写操作带审计字段。

### 2.3 前后端最小联调
- [x] 设置接口（模型、思考等级、全局交易开关等）
  - 完成说明：已实现 `GET/PUT /api/v1/settings/runtime`，含校验、审计、事件广播。
- [x] 基础日志接口 / SSE 通道
  - 完成说明：已实现 `GET /api/v1/logs` 与 `GET /api/v1/events/stream`（心跳 + 日志事件）。
- [ ] 前端从 mock 切到真实接口（第一批页面）
  - 完成说明：后端联调接口与兼容占位接口已就绪，待在“可用 PostgreSQL/Redis”环境执行前端 API 模式全链路回归。

## 3. Phase 3：Telegram 接入与消息链路

### 3.1 频道采集能力
- [ ] 支持 `t.me/s/<channel>` 轮询抓取（默认 30s）
- [ ] 频道级 cursor 独立维护
- [ ] 频道配置热更新（新增/编辑/停用无需重启）

### 3.2 消息处理能力
- [ ] 落库 `raw_messages` 原始抓取记录
- [ ] 标准化为 `normalized_messages`
- [ ] 识别编辑消息并生成 `message_versions`
- [ ] 去重与幂等（重复抓取不重复进入决策）

## 4. Phase 4：AI 决策与人工确认

### 4.1 上下文构建
- [ ] 严格单频道上下文拼装
- [ ] 注入最近消息、未完成信号、虚拟子仓位状态
- [ ] 生成可审计上下文快照

### 4.2 决策与控门
- [ ] 结构化 JSON 决策输出（动作、价格、理由、置信度）
- [ ] 支持模型与思考等级运行时切换
- [ ] 新开仓低置信度进入人工确认
- [ ] 跟进动作默认自动执行（受策略边界约束）

### 4.3 人工确认闭环
- [ ] 生成 `manual_reviews` 待确认项
- [ ] 支持确认执行 / 拒绝执行 / 失效处理
- [ ] 幂等保证（同一确认项最多执行一次）

## 5. Phase 5：OKX 模拟盘执行与状态同步

### 5.1 执行层
- [ ] OKX 模拟盘凭据接入与环境隔离
- [ ] 标的、余额、持仓、挂单查询
- [ ] 执行规划（动作拆单、止盈止损映射）
- [ ] 下单/撤单/改单/条件单等核心动作
- [ ] 未成交 5h 自动撤单

### 5.2 同步层
- [ ] 私有 WebSocket 作为主同步通道
- [ ] REST 补偿同步与对账
- [ ] 订单/成交/持仓/账户状态持续回写

## 6. Phase 6：虚拟子仓位账本与 PnL

### 6.1 账本模型
- [ ] 建立 `virtual_positions` 与生命周期状态
- [ ] 建立 `virtual_position_events` 事件流
- [ ] 建立 `pnl_snapshots` 快照

### 6.2 账本映射与核对
- [ ] 决策 -> 执行计划 -> 真实订单 -> 成交 -> 账本事件全链路映射
- [ ] 多频道同标的隔离核算
- [ ] 偏差修正（以真实成交为准）

## 7. Phase 7：OpenClaw 集成

### 7.1 查询与日志
- [ ] Telegram topic 输出日志摘要
- [ ] 支持自然语言查询状态

### 7.2 控制动作
- [ ] 低风险动作白名单（暂停/恢复频道等）
- [ ] 高风险动作二次确认（与 Web 边界一致）
- [ ] 所有动作写入审计链路

### 7.3 技能交付
- [ ] 输出项目专用 OpenClaw skill

## 8. Phase 8：强化、上线准备与验收

### 8.1 可靠性与可观测性
- [ ] 完整 `correlation_id` 链路追踪
- [ ] 健康检查接口（`/health/live`、`/health/ready`、`/health/deps`）
- [ ] 重试、幂等、恢复、补偿策略补齐

### 8.2 安全与运维
- [ ] 日志与返回内容脱敏检查
- [ ] 模拟盘/实盘配置与数据严格隔离
- [ ] 备份与恢复演练（数据库、关键快照）

### 8.3 V1 验收收口
- [ ] 功能链路验收通过（采集 -> 决策 -> 执行 -> 同步 -> 账本 -> 控制台 -> OpenClaw）
- [ ] 边界验收通过（单用户、仅 OKX、仅合约、仅网页抓取等）
- [ ] 安全/可靠性/可观测性验收通过
- [ ] 模拟盘稳定运行后，再进入实盘准备

---

## 建议的日常更新规则

- [x] 每天更新一次本清单状态
- [x] 每完成一个子步骤，立即勾选并补一行“完成说明”
- [ ] 每个 Phase 结束时，追加“阶段复盘（风险/遗留/下一步）”

## 最近进度记录

### 2026-03-22

- 完成前端 API 基础层：统一包络解包、错误码映射、幂等键与审计透传头。
- 完成前端 SSE 基础层：事件客户端、重连退避、降级轮询与顶栏状态可视化。
- 完成 `api/mock` 运行时切换：`VITE_DATA_SOURCE`、`VITE_API_BASE_URL`、`VITE_ENABLE_SSE`。
- 完成频道页写操作联调骨架：新增/编辑/启停走 API（`api` 模式）并具备错误提示。
- 完成设置页风险入口收敛：运行状态改为只读展示，环境切换/全局交易仅可在危险操作区（含二次确认）执行。
- 完成设置页敏感配置策略收敛：AI API Key / Base URL 归入部署级机密，不在前端页面明文填写。
- 完成 Phase 3.1~3.3 AI 测试执行并留存证据（见 `docs/project-phase-test-checklist.md` 第 13 节）。
- 完成 Phase 2 后端骨架开发：新增 `apps/api`，落地健康检查、认证/会话、设置、日志、SSE、审计与兼容联调接口。
- 完成后端依赖安装与单测冒烟：`python -m pip install -e .[dev]`、`python -m pytest tests/unit -q`（8/8 通过）。
- 完成健康检查接口冒烟：`/health/live=200`、`/health/ready=503(deps down)`、`/health/deps=200`，行为符合“依赖异常不可假成功”预期。
- 完成 Phase 2 集成测试环境搭建：本地 Docker 启动 `postgres:16`、`redis:7`，`python -m pytest tests/integration/test_phase2_foundation.py -q` 通过。
- 完成后端当前测试集回归：`cd apps/api && python -m pytest tests -q`（11/11 通过）。
