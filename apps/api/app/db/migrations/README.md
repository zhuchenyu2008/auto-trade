# Alembic migrations

Phase 2 keeps the folder reserved for Alembic-based schema migrations.

Current implementation can auto-create baseline tables in development mode
(`AUTO_CREATE_SCHEMA=true`) to speed up local integration.
